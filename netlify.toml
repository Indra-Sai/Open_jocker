[build]
  base    = "client"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: every path that doesn't match a static asset serves index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
